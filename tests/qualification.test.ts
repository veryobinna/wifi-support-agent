import { describe, it } from "vitest";

describe("qualification", () => {
  it.todo("marks a multi-device internet outage as potentially appropriate for reboot");
  it.todo("exits gracefully when only one device is affected");
  it.todo("exits gracefully when there is a known ISP outage");
});
